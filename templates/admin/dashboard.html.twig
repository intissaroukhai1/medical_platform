{% extends 'admin/_layout_admin.html.twig' %}

{% block admin_content %}

<div class="header">
    <h1>Dashboard Admin</h1>

    <div class="user-profile">
    <div class="user-info">
        <div class="user-name">
            {{ app.user.nom ?? 'Admin' }}
        </div>
        <div class="user-role">
            Administrateur
        </div>
    </div>

    <div class="user-avatar">
        {% if app.user.photoProfil %}
            <img
                src="{{ asset('uploads/photos/' ~ app.user.photoProfil) }}"
                alt="Photo de profil"
                style="width:100%; height:100%; border-radius:50%; object-fit:cover;"
            >
        {% else %}
            {{ app.user.nom
                ? app.user.nom|slice(0,1)|upper
                : app.user.email|slice(0,1)|upper
            }}
        {% endif %}
    </div>
</div>
</div>

<div class="stats-grid">
    <div class="stat-card">
        <div class="stat-label">Utilisateurs Totaux</div>
<div class="stat-value">{{ usersCount }}</div>        <div class="stat-change">↑ 12% ce mois</div>
    </div>

    <div class="stat-card">
        <div class="stat-label">Médecins Actifs</div>
        <div class="stat-value">{{ medecinsCount }}</div>
    </div>

    <div class="stat-card">
        <div class="stat-label">Consultations</div>
<div class="stat-value">{{ consultationsCount }}</div>    </div>

    <div class="stat-card">
        <div class="stat-label">Revenus</div>
<div class="stat-value">
    {{ revenus|number_format(0, '.', ' ') }} DH
</div>    </div>
</div>

{% endblock %}
