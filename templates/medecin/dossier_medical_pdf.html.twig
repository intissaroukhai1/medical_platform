<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dossier Médical</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: #f5f5f5;
            padding: 20px;
            color: #333;
        }

        .page {
            max-width: 1000px;
            margin: 0 auto;
            background: white;
            padding: 0;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
        }

        /* Header avec fond violet */
        .header {
            background: #667eea;
            color: white;
            padding: 40px 30px;
            text-align: center;
            border-bottom: 5px solid #5568d3;
        }

        .header h1 {
            font-size: 32px;
            font-weight: 700;
            margin-bottom: 8px;
        }

        .header .subtitle {
            font-size: 14px;
            opacity: 0.95;
            font-style: italic;
        }

        /* Contenu */
        .content {
            padding: 30px;
        }

        .section {
            margin-bottom: 35px;
            page-break-inside: avoid;
        }

        .section-header {
            background: #667eea;
            color: white;
            padding: 12px 20px;
            margin-bottom: 20px;
            font-size: 18px;
            font-weight: 600;
            border-radius: 5px;
        }

        .section-header .icon {
            margin-right: 8px;
        }

        /* Cartes d'information */
        .info-card {
            background: #f8f9fa;
            border: 2px solid #e9ecef;
            border-radius: 8px;
            padding: 20px;
            margin-bottom: 15px;
        }

        .info-row {
            margin-bottom: 12px;
            line-height: 1.6;
        }

        .info-row:last-child {
            margin-bottom: 0;
        }

        .info-label {
            font-weight: 700;
            color: #495057;
            display: inline-block;
            min-width: 140px;
        }

        .info-value {
            color: #212529;
        }

        /* Tableaux */
        .table-wrapper {
            border: 2px solid #dee2e6;
            border-radius: 8px;
            overflow: hidden;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            background: white;
        }

        thead {
            background: #667eea;
            color: white;
        }

        th {
            padding: 14px;
            text-align: left;
            font-weight: 600;
            font-size: 13px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        td {
            padding: 14px;
            border-bottom: 1px solid #dee2e6;
        }

        tbody tr:last-child td {
            border-bottom: none;
        }

        tbody tr:nth-child(even) {
            background: #f8f9fa;
        }

        .empty-row {
            text-align: center;
            color: #6c757d;
            font-style: italic;
            padding: 30px !important;
        }

        /* Badges de statut */
        .badge {
            display: inline-block;
            padding: 6px 12px;
            border-radius: 15px;
            font-size: 12px;
            font-weight: 600;
        }

        .badge-confirme {
            background: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }

        .badge-annule {
            background: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }

        .badge-en-attente {
            background: #fff3cd;
            color: #856404;
            border: 1px solid #ffeaa7;
        }

        /* Footer */
        .footer {
            background: #f8f9fa;
            padding: 20px 30px;
            text-align: center;
            border-top: 3px solid #667eea;
            font-size: 12px;
            color: #6c757d;
            margin-top: 30px;
        }

        /* Print styles */
        @media print {
            body {
                background: white;
                padding: 0;
            }
            
            .page {
                box-shadow: none;
                max-width: 100%;
            }
        }
    </style>
</head>
<body>
    <div class="page">
        <!-- Header principal -->
        <div class="header">
            <h1>Dossier Médical</h1>
            <div class="subtitle">Document confidentiel</div>
        </div>

        <div class="content">
            <!-- Section Patient -->
            <div class="section">
                <div class="section-header">
                    <span class="icon">👤</span> Informations Patient
                </div>
                <div class="info-card">
                    <div class="info-row">
                        <span class="info-label">Nom complet :</span>
                        <span class="info-value">{{ patient.prenom }} {{ patient.nom }}</span>
                    </div>
                    <div class="info-row">
                        <span class="info-label">Email :</span>
                        <span class="info-value">{{ patient.email }}</span>
                    </div>
                    <div class="info-row">
                        <span class="info-label">Téléphone :</span>
                        <span class="info-value">{{ patient.telephone|default('Non renseigné') }}</span>
                    </div>
                </div>
            </div>

            <!-- Section Médecin -->
            <div class="section">
                <div class="section-header">
                    <span class="icon">👨‍⚕️</span> Médecin Traitant
                </div>
                <div class="info-card">
                    <div class="info-row">
                        <span class="info-label">Praticien :</span>
                        <span class="info-value">Dr {{ medecin.nom }} {{ medecin.prenom }}</span>
                    </div>
                </div>
            </div>

            <!-- Section Rendez-vous -->
            <div class="section">
                <div class="section-header">
                    <span class="icon">📅</span> Historique des Rendez-vous
                </div>
                <div class="table-wrapper">
                    <table>
                        <thead>
                            <tr>
                                <th>Date et Heure</th>
                                <th>Statut</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for rdv in rdvs %}
                                <tr>
                                    <td>{{ rdv.date|date('d/m/Y à H:i') }}</td>
                                    <td>
                                        <span class="badge badge-{{ rdv.statut|lower|replace({' ': '-'}) }}">
                                            {{ rdv.statut }}
                                        </span>
                                    </td>
                                </tr>
                            {% else %}
                                <tr>
                                    <td colspan="2" class="empty-row">
                                        Aucun rendez-vous enregistré
                                    </td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>

            <!-- Section Ordonnances -->
            <div class="section">
                <div class="section-header">
                    <span class="icon">💊</span> Ordonnances
                </div>
                <div class="table-wrapper">
                    <table>
                        <thead>
                            <tr>
                                <th style="width: 25%;">Date de Création</th>
                                <th>Contenu</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for ordonnance in ordonnances %}
                                <tr>
                                    <td>{{ ordonnance.createdAt|date('d/m/Y') }}</td>
                                    <td>{{ ordonnance.contenu }}</td>
                                </tr>
                            {% else %}
                                <tr>
                                    <td colspan="2" class="empty-row">
                                        Aucune ordonnance disponible
                                    </td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        <!-- Footer -->
        <div class="footer">
            Document généré le {{ "now"|date('d/m/Y à H:i') }}
        </div>
    </div>
</body>
</html>