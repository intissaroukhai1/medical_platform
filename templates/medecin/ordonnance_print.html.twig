<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Ordonnance</title>

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .prescription-container {
            background: white;
            max-width: 800px;
            width: 100%;
            border-radius: 15px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            overflow: hidden;
            position: relative;
        }

        .watermark {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) rotate(-45deg);
            font-size: 80px;
            color: rgba(102, 126, 234, 0.05);
            font-weight: 900;
            pointer-events: none;
            white-space: nowrap;
        }

        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 30px;
            position: relative;
            text-align: center;
        }

        .header::after {
            content: '';
            position: absolute;
            bottom: -20px;
            left: 0;
            right: 0;
            height: 20px;
            background: white;
            border-radius: 50% 50% 0 0 / 100% 100% 0 0;
        }

        .doctor {
            font-weight: 700;
            font-size: 26px;
            margin-bottom: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 15px;
        }

        .doctor::before {
            content: '⚕️';
            font-size: 30px;
        }

        .ville {
            font-size: 15px;
            opacity: 0.9;
            margin-top: 8px;
        }

        .content-wrapper {
            padding: 40px;
        }

        .patient {
            background: linear-gradient(135deg, #f8f9ff 0%, #e8ecff 100%);
            padding: 25px;
            border-radius: 12px;
            border-left: 5px solid #667eea;
            margin-bottom: 30px;
            box-shadow: 0 2px 10px rgba(102, 126, 234, 0.1);
        }

        .patient strong {
            color: #667eea;
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            display: inline-block;
            min-width: 120px;
        }

        .patient-line {
            margin-bottom: 12px;
            font-size: 16px;
            color: #333;
        }

        .patient-line:last-child {
            margin-bottom: 0;
        }

        .section-title {
            font-size: 18px;
            font-weight: 700;
            color: #667eea;
            margin-bottom: 15px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            border-bottom: 3px solid #667eea;
            padding-bottom: 8px;
            display: inline-block;
        }

        .content {
            background: #fafafa;
            padding: 25px;
            border-radius: 10px;
            border: 2px dashed #ddd;
            min-height: 200px;
            line-height: 1.9;
            font-size: 16px;
            color: #333;
            white-space: pre-line;
            margin-bottom: 30px;
        }

        .footer {
            display: flex;
            justify-content: space-between;
            align-items: flex-end;
            padding-top: 30px;
            border-top: 2px solid #eee;
            margin-top: 40px;
        }

        .date-footer {
            color: #666;
            font-size: 14px;
            font-style: italic;
        }

        .signature-box {
            text-align: center;
        }

        .signature-box p {
            font-size: 14px;
            color: #666;
            margin-bottom: 15px;
            font-weight: 600;
        }

        .stamp-area {
            width: 90px;
            height: 90px;
            border: 2px dashed #ccc;
            border-radius: 50%;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            font-size: 11px;
            color: #999;
            text-align: center;
            padding: 10px;
            margin-bottom: 10px;
        }

        .signature-line {
            width: 200px;
            border-bottom: 2px solid #333;
            margin: 40px auto 0;
        }

        @media print {
            body {
                background: white;
                padding: 0;
                display: block;
            }

            .prescription-container {
                box-shadow: none;
                border-radius: 0;
                max-width: 100%;
            }

            .header::after {
                display: none;
            }
        }

        @media (max-width: 600px) {
            body {
                padding: 10px;
            }

            .content-wrapper {
                padding: 20px;
            }

            .doctor {
                font-size: 22px;
            }

            .footer {
                flex-direction: column;
                gap: 20px;
                align-items: center;
            }
        }
    </style>
</head>

<body onload="window.print()">

<div class="prescription-container">
    <div class="watermark">ORDONNANCE</div>
    
    <div class="header">
        <div class="doctor">
            Dr {{ ordonnance.medecin.prenom }} {{ ordonnance.medecin.nom }}
        </div>
        <div class="ville">
            📍 {{ ordonnance.medecin.ville ?? '' }}
        </div>
    </div>

    <div class="content-wrapper">
        <div class="patient">
            <div class="patient-line">
                <strong>Patient :</strong>
                {{ ordonnance.patient.prenom }} {{ ordonnance.patient.nom }}
            </div>
            <div class="patient-line">
                <strong>Date :</strong>
                {{ ordonnance.createdAt|date('d/m/Y') }}
            </div>
        </div>

        <div class="section-title">📋 Ordonnance</div>

        <div class="content">{{ ordonnance.contenu }}</div>

        <div class="footer">
            <div class="date-footer">
                Document généré le {{ ordonnance.createdAt|date('d/m/Y') }}
            </div>
            
            <div class="signature-box">
                <p>Signature et Cachet :</p>
                <div class="stamp-area">Cachet du médecin</div>
                <div class="signature-line"></div>
            </div>
        </div>
    </div>
</div>

</body>
</html>