{% extends 'base.html.twig' %}

{% block title %}{% block page_title %}Dashboard Patient{% endblock %} - Dawini{% endblock %}

{% block stylesheets %}
{{ parent() }}
<style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap');

    :root {
        --primary: #0066CC;
        --primary-dark: #0052A3;
        --primary-light: #3B82F6;
        --secondary: #00C9A7;
        --secondary-dark: #00B896;
        --accent: #FF6B6B;
        --bg: #F0F4F8;
        --surface: #FFFFFF;
        --text-primary: #1E293B;
        --text-secondary: #64748B;
        --border: #E2E8F0;
        --radius: 16px;
        --shadow-sm: 0 2px 8px rgba(0, 102, 204, 0.08);
        --shadow-md: 0 8px 24px rgba(0, 102, 204, 0.12);
        --shadow-lg: 0 16px 40px rgba(0, 102, 204, 0.16);
    }

    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    body {
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
        background: linear-gradient(135deg, #E0F2FE 0%, #DBEAFE 50%, #EFF6FF 100%);
        min-height: 100vh;
    }

    /* ============ SIDEBAR MODERNE ============ */
    .patient-sidebar {
        width: 280px;
        background: linear-gradient(180deg, #0066CC 0%, #0052A3 100%);
        border-right: none;
        padding: 2rem 0;
        min-height: 100vh;
        box-shadow: 4px 0 24px rgba(0, 102, 204, 0.15);
        position: relative;
        overflow: hidden;
    }

    .patient-sidebar::before {
        content: '';
        position: absolute;
        top: -50%;
        right: -50%;
        width: 200%;
        height: 200%;
        background: radial-gradient(circle, rgba(255, 255, 255, 0.1) 0%, transparent 70%);
        animation: pulse 4s ease-in-out infinite;
    }

    @keyframes pulse {
        0%, 100% { opacity: 0.5; }
        50% { opacity: 1; }
    }

    .sidebar-logo {
        padding: 0 1.5rem 2rem;
        font-size: 2rem;
        font-weight: 800;
        color: white;
        border-bottom: 2px solid rgba(255, 255, 255, 0.2);
        margin-bottom: 2rem;
        display: flex;
        align-items: center;
        gap: 0.75rem;
        text-shadow: 0 2px 10px rgba(0, 0, 0, 0.2);
        position: relative;
        z-index: 1;
    }

    .sidebar-logo::before {
        content: "⚕️";
        font-size: 2.5rem;
        filter: drop-shadow(0 4px 8px rgba(255, 255, 255, 0.3));
    }

    .sidebar-menu {
        list-style: none;
        padding: 0 1rem;
        margin: 0;
        position: relative;
        z-index: 1;
    }

    .sidebar-menu li {
        margin-bottom: 0.5rem;
    }

    .sidebar-menu a {
        display: flex;
        align-items: center;
        padding: 1rem 1.25rem;
        color: rgba(255, 255, 255, 0.85);
        text-decoration: none;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        gap: 1rem;
        cursor: pointer;
        border-radius: 12px;
        font-weight: 500;
        font-size: 0.95rem;
        position: relative;
        overflow: hidden;
    }

    .sidebar-menu a::before {
        content: '';
        position: absolute;
        left: 0;
        top: 0;
        height: 100%;
        width: 4px;
        background: white;
        transform: translateX(-100%);
        transition: transform 0.3s ease;
        border-radius: 0 4px 4px 0;
        box-shadow: 0 0 15px rgba(255, 255, 255, 0.6);
    }

    .sidebar-menu a::after {
        content: '';
        position: absolute;
        top: 50%;
        left: 50%;
        width: 0;
        height: 0;
        border-radius: 50%;
        background: rgba(255, 255, 255, 0.2);
        transform: translate(-50%, -50%);
        transition: width 0.4s, height 0.4s;
    }

    .sidebar-menu a:hover::after {
        width: 300px;
        height: 300px;
    }

    .sidebar-menu a:hover {
        background: rgba(255, 255, 255, 0.15);
        color: white;
        transform: translateX(4px);
        backdrop-filter: blur(10px);
    }

    .sidebar-menu a:hover::before {
        transform: translateX(0);
    }

    .sidebar-menu a.active {
        background: rgba(255, 255, 255, 0.25);
        color: white;
        font-weight: 600;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
        backdrop-filter: blur(10px);
    }

    .sidebar-menu a.active::before {
        transform: translateX(0);
    }

    .sidebar-menu a span:first-child {
        font-size: 1.25rem;
    }

    .sidebar-divider {
        height: 1px;
        background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
        margin: 1rem 0;
    }

    /* ============ MAIN CONTENT ============ */
    .patient-main {
        flex: 1;
        padding: 2rem;
    }

    /* ============ HEADER MODERNE ============ */
    .patient-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 2rem;
        background: rgba(255, 255, 255, 0.95);
        padding: 1.75rem 2rem;
        border-radius: var(--radius);
        box-shadow: var(--shadow-md);
        backdrop-filter: blur(20px);
        border: 1px solid rgba(255, 255, 255, 0.8);
        position: relative;
        overflow: hidden;
    }

    .patient-header::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 4px;
        background: linear-gradient(90deg, #0066CC 0%, #00C9A7 100%);
    }

    .patient-header h1 {
        font-size: 1.875rem;
        font-weight: 700;
        margin: 0;
        background: linear-gradient(135deg, #0066CC 0%, #00C9A7 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .user-profile {
        display: flex;
        align-items: center;
        gap: 1rem;
        padding: 0.5rem 1rem;
        background: rgba(0, 102, 204, 0.05);
        border-radius: 12px;
        transition: all 0.3s;
    }

    .user-profile:hover {
        background: rgba(0, 102, 204, 0.1);
        transform: translateY(-2px);
        box-shadow: 0 4px 12px rgba(0, 102, 204, 0.15);
    }

    .user-info {
        text-align: right;
    }

    .user-name {
        font-weight: 600;
        color: var(--text-primary);
        margin: 0;
        font-size: 0.95rem;
    }

    .user-role {
        font-size: 0.8rem;
        background: linear-gradient(135deg, #0066CC, #00C9A7);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        font-weight: 600;
        text-transform: uppercase;
        margin: 0.125rem 0 0 0;
    }

    .user-avatar {
        width: 48px;
        height: 48px;
        border-radius: 50%;
        background: linear-gradient(135deg, #0066CC 0%, #00C9A7 100%);
        color: white;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: 700;
        font-size: 1.1rem;
        box-shadow: 0 4px 12px rgba(0, 102, 204, 0.3);
        position: relative;
        overflow: hidden;
    }

    .user-avatar::before {
        content: '';
        position: absolute;
        top: -50%;
        right: -50%;
        width: 200%;
        height: 200%;
        background: radial-gradient(circle, rgba(255, 255, 255, 0.3) 0%, transparent 70%);
    }

    /* ============ BUTTONS ============ */
    .btn {
        padding: 0.625rem 1.5rem;
        border: none;
        border-radius: 10px;
        cursor: pointer;
        font-size: 0.875rem;
        font-weight: 600;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        position: relative;
        overflow: hidden;
    }

    .btn::before {
        content: '';
        position: absolute;
        top: 50%;
        left: 50%;
        width: 0;
        height: 0;
        border-radius: 50%;
        background: rgba(255, 255, 255, 0.3);
        transform: translate(-50%, -50%);
        transition: width 0.6s, height 0.6s;
    }

    .btn:hover::before {
        width: 300px;
        height: 300px;
    }

    .btn-primary {
        background: linear-gradient(135deg, #0066CC 0%, #0052A3 100%);
        color: white;
        box-shadow: 0 4px 15px rgba(0, 102, 204, 0.3);
    }

    .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 20px rgba(0, 102, 204, 0.4);
    }

    .btn-secondary {
        background: linear-gradient(135deg, #00C9A7 0%, #00B896 100%);
        color: white;
        box-shadow: 0 4px 15px rgba(0, 201, 167, 0.3);
    }

    .btn-secondary:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 20px rgba(0, 201, 167, 0.4);
    }

    /* ============ ALERTS ============ */
    .alert {
        padding: 1rem 1.25rem;
        border-radius: 12px;
        margin-bottom: 1.5rem;
        display: flex;
        align-items: center;
        gap: 0.75rem;
        font-weight: 500;
        font-size: 0.9375rem;
        border-left: 4px solid;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
    }

    .alert-success {
        background: linear-gradient(135deg, #ECFDF5, #D1FAE5);
        color: #065F46;
        border-color: #10B981;
    }

    .alert-danger {
        background: linear-gradient(135deg, #FEF2F2, #FEE2E2);
        color: #991B1B;
        border-color: #EF4444;
    }

    .alert-info {
        background: linear-gradient(135deg, #EFF6FF, #DBEAFE);
        color: #1E40AF;
        border-color: #0066CC;
    }

    .alert-warning {
        background: linear-gradient(135deg, #FFFBEB, #FEF3C7);
        color: #92400E;
        border-color: #F59E0B;
    }

    /* ============ RESPONSIVE ============ */
    @media (max-width: 768px) {
        .patient-sidebar {
            width: 100%;
            min-height: auto;
            border-bottom: 2px solid rgba(255, 255, 255, 0.2);
        }

        .patient-main {
            padding: 1.25rem;
        }

        .patient-header {
            flex-direction: column;
            gap: 1.25rem;
            text-align: center;
            padding: 1.5rem;
        }

        .patient-header h1 {
            font-size: 1.5rem;
        }

        .sidebar-menu {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
            gap: 0.5rem;
            padding: 1rem;
        }

        .sidebar-menu li {
            margin-bottom: 0;
        }

        .sidebar-menu a {
            flex-direction: column;
            text-align: center;
            padding: 1rem 0.5rem;
            gap: 0.5rem;
            font-size: 0.8125rem;
        }

        .sidebar-menu a span:first-child {
            font-size: 1.5rem;
        }
    }
</style>
{% endblock %}

{% block sidebar %}
<aside class="patient-sidebar">
    <div class="sidebar-logo">Dawini</div>
    
    <ul class="sidebar-menu">
        <li>
            <a href="{{ path('patient_dashboard') }}" 
               class="{{ app.request.attributes.get('_route') == 'patient_dashboard' ? 'active' : '' }}">
                <span>📊</span>
                <span>Aperçu</span>
            </a>
        </li>
        <li>
            <a href="{{ path('patient_rendezvous') }}"
   class="{{ app.request.attributes.get('_route') starts with 'patient_rendezvous' ? 'active' : '' }}">
    <span>📅</span>
    <span>Rendez-vous</span>
</a>
        </li>
       {# <li>
            <a href="#" 
               class="{{ app.request.attributes.get('_route') starts with 'patient_medical' ? 'active' : '' }}">
                <span>📋</span>
                <span>Dossier Médical</span>
            </a>
        </li>#}
        <li>
          <a href="{{ path('patient_ordonnances') }}"
   class="{{ app.request.attributes.get('_route') starts with 'patient_ordonnances' ? 'active' : '' }}">
    <span>💊</span>
    <span>Ordonnances</span>
</a>

        </li>
       {# <li>
            <a href="#" 
               class="{{ app.request.attributes.get('_route') starts with 'patient_search' ? 'active' : '' }}">
                <span>🔍</span>
                <span>Chercher Médecin</span>
            </a>
        </li>#}
        <li>
            <a href="#" 
               class="{{ app.request.attributes.get('_route') starts with 'patient_profile' ? 'active' : '' }}">
                <span>👤</span>
                <span>Mon Profil</span>
            </a>
        </li>
        
        <div class="sidebar-divider"></div>
        
        <li>
            <a href="{{ path('app_logout') }}">
                <span>🚪</span>
                <span>Déconnexion</span>
            </a>
        </li>
    </ul>
</aside>
{% endblock %}

{% block content %}
<div class="patient-main">
    <div class="patient-header">
        <h1>{% block header_title %}Dashboard Patient{% endblock %}</h1>
        <div class="user-profile">
            <div class="user-info">
                <div class="user-name">{{ app.user.prenom }} {{ app.user.nom }}</div>
                <div class="user-role">Patient</div>
            </div>
            <div class="user-avatar">
                {{ app.user.prenom|first|upper }}{{ app.user.nom|first|upper }}
            </div>
        </div>
    </div>

    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="alert alert-{{ label }}">{{ message }}</div>
        {% endfor %}
    {% endfor %}

    {% block page_content %}{% endblock %}
</div>
{% endblock %}